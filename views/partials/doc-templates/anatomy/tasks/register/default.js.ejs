<h1 id="tasks-register-default-js">tasks/register/default.js</h1>
<docmeta name="displayName" value="default.js">

<p>The default grunt task list.  sails will run this if there is no task list file matching NODE_ENV (eg. NODE_ENV = QA and QA.js exists) and no other environment is set (--prod, etc.).</p>
<pre><code class="nohighlight">module.exports = function (grunt) {
  grunt.registerTask(&apos;default&apos;, [&apos;compileAssets&apos;, &apos;linkAssets&apos;,  &apos;watch&apos;]);
};
</code></pre><!-- __LANG=%%__ -->
</docmeta>