/**
 * `raw-component`
 *
 * A module that provides basic utilities for declaratively registering UI components,
 * binding DOM events, and managing memory.  Sort of like an extremely basic Backbone view,
 * but with a purely functional (rather than object-oriented) implementation.
 */

angular.module('raw-component', ['negotiate-keyboard-event']);
